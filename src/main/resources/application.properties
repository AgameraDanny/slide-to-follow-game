spring.application.name=treehugger

# ======================================================
# OPTION 1: LOCAL DATABASE (Active Default)
# ======================================================
# These values are used automatically if you just run 'mvn spring-boot:run'
# This is safe to push to GitHub because it contains no secrets.

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/treehugger_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ======================================================
# OPTION 2: TiDB CLOUD (Online)
# ======================================================
# To connect to TiDB, do NOT edit this file (to keep it safe).
# Instead, run the command below in your terminal.
#
DB_URL="jdbc:mysql://gateway01.eu-central-1.prod.aws.tidbcloud.com:4000/treehugger_db?useSSL=true&requireSSL=true&sslMode=VERIFY_IDENTITY&serverTimezone=UTC&createDatabaseIfNotExist=true"
DB_USERNAME="3EcEyk7CZfWvp8J.root"
DB_PASSWORD="wbXd4VJcIgePaIpN"

# ======================================================
# JPA / HIBERNATE SETTINGS
# ======================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ======================================================
# WEB SETTINGS
# ======================================================
spring.mvc.static-path-pattern=/**