spring.application.name=treehugger

# ======================================================
# DATABASE CONFIGURATION (Defaults to TiDB Cloud)
# ======================================================
# The values after the colon ':' are the defaults.

spring.datasource.url=${DB_URL:jdbc:mysql://gateway01.eu-central-1.prod.aws.tidbcloud.com:4000/treehugger_db?useSSL=true&requireSSL=true&sslMode=VERIFY_IDENTITY&serverTimezone=UTC&createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:3EcEyk7CZfWvp8J.root}
spring.datasource.password=${DB_PASSWORD:wbXd4VJcIgePaIpN}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ======================================================
# JPA / HIBERNATE SETTINGS
# ======================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ======================================================
# WEB SETTINGS
# ======================================================
spring.mvc.static-path-pattern=/**
spring.thymeleaf.cache=false